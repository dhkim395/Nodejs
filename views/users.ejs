<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>사용자 목록</title>
  </head>
  <body>
    <h2>사용자 목록</h2>
    <ul>
      <!-- 안에 html을 넣을 때는 <% %>을 닫고 다시 열고 닫는다 -->
      <% users.forEach(function (user) { %>
      <li><%=user%></li>
      <%}); %>
    </ul>
  </body>
</html>
